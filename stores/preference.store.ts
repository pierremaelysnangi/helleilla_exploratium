/**
 * @file Store Zustand des préférences utilisateur (à implémenter).
 *
 * Rôle prévu : persister les réglages de l'interface (thème, volume par
 * défaut, langue, etc.), éventuellement via le middleware `persist`.
 */
