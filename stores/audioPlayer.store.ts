/**
 * @file Store Zustand du lecteur audio (à implémenter).
 *
 * Rôle prévu : état global du lecteur — piste courante, file d'attente,
 * lecture/pause, position, volume — partagé entre les composants
 * sans prop drilling.
 */
