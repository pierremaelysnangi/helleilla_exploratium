/**
 * @file Hook React `useMediaQuery` (à implémenter).
 *
 * Rôle prévu : s'abonner à une media query CSS (ex. `(max-width: 768px)`)
 * et retourner un booléen réactif indiquant si elle est active,
 * pour adapter le rendu côté client.
 */
