/**
 * @file Types générés de la base de données (à compléter).
 *
 * Rôle prévu : contenir les types générés automatiquement à partir du
 * schéma (ex. sortie de `drizzle-kit` ou Supabase) : lignes, inserts,
 * résultats de requêtes — à ne PAS éditer manuellement si généré.
 */
