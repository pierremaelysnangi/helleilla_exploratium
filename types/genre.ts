/**
 * @file Types TypeScript liés aux genres (à compléter).
 *
 * Rôle prévu : définir les types/DTO utilisés par l'UI pour les genres
 * (ex. arbre hiérarchique genre → sous-genres), en complément des types
 * inférés automatiquement par Drizzle depuis le schéma.
 */
