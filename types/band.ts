/**
 * @file Types TypeScript liés aux groupes musicaux (bands) (à compléter).
 *
 * Rôle prévu : définir les types/DTO utilisés par l'UI pour les groupes
 * (ex. variantes enrichies avec albums ou genres), en complément des types
 * inférés automatiquement par Drizzle depuis le schéma.
 */
