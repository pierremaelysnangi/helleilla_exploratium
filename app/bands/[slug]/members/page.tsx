/**
 * Page des membres d'un groupe (/bands/[slug]/members).
 * Page placeholder : le paramètre [slug] identifiera le groupe dont on
 * affiche les membres (actuels et passés) une fois la logique implémentée.
 */
export default function BandsSlugMembersPage() {
  return (
    <main className="container mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold">BandsSlugMembers</h1>
      <p className="text-muted-foreground mt-2">
        Page en construction — /bands/[slug]/members
      </p>
    </main>
  );
}
