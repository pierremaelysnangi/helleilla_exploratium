/**
 * Page de détail d'un membre (/members/[slug]).
 * Page placeholder : le paramètre dynamique [slug] permettra de charger
 * les informations du membre correspondant une fois la logique de données implémentée.
 */
export default function MembersSlugPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold">MembersSlug</h1>
      <p className="text-muted-foreground mt-2">
        Page en construction — /members/[slug]
      </p>
    </section>
  );
}
